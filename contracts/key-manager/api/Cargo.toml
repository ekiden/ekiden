[package]
name = "key-manager-api"
version = "0.1.0"
authors = ["Jernej Kos <jernej@kos.mx>"]
build = "build.rs"

[dependencies]
ekiden-core-common = { path = "../../../core/common" }
protobuf = "1.4.3"

[build-dependencies]
ekiden-tools = { path = "../../../tools" }
